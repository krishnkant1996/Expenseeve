// require('../start');
// require('../init');

const app = require('express')();
const expense = require('../schemas/expense');

// app.post('/expense', (req, res) => {
//     const { body } = req;
//     expense.saveExpense(body, (err, data) => { console.log(err ? 34 : (31, err, data)) });
//     res.status(200).send(body)
// })
